
<div class="container text-center">
    <div class="row">
        {{#each products}}

             <img class="images" src={{this.imageurl}} >
             <h1> {{this.productname}} </h1>
    {{/each}}
</div>
</div>
